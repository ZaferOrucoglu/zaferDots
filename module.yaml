# Module for hyprDots
description: Hyprland Dots

packages:
  - packages.yaml

conflicts: []

# Enabling dots sync
# This auto-syncs dotfiles/ to ~/.config/
dotfiles_sync: true

pre_install_hook: ""

post_install_hook: ""
